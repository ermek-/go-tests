# Base URL of the API under test (no trailing slash)
BASE_URL=http://api.dev.pkmt.tech

# Auth endpoint (will be appended to BASE_URL). Leave empty if no auth is needed.
AUTH_ENDPOINT=/login/users/tokens

# Credentials for auth request
USERNAME=admin
PASSWORD=admin

# Resource endpoints (adjust to your API)
PRODUCTS_ENDPOINT=/Nomenclature/v1/nomenclatures
PRODUCT_ENDPOINT=/Nomenclature/v1/nomenclatures

# --- Logging ---
LOG_HTTP=1
LOG_HTTP_BODY=1
LOG_MAX_BYTES=8192
LOG_DIR=./logs
